const { N8NPropertiesBuilder: BaseN8NPropertiesBuilder }: any = require('@devlikeapro/n8n-openapi-node');

export default class N8NPropertiesBuilder extends BaseN8NPropertiesBuilder {
  constructor(doc: any, config: any) {
    super(doc, config);
  }

  build() {
    return super.build();
  }
}
